/*

A namespace is a compartment where variables or functions are stored
to prevent errors due to the same errors.



std - standard namespace.
:: - scope resolution operator
cout - console output (object of iostream class)
<< - stream insertion operator.
endl - end of line

*/


/*
Special Characters

# denote a preprocessor directive.
<> enloses library

<< Extraction Op. Outputs to the console.
>> Input Op. Inputs from the console


*/

/*
Variable types
int - integer
double - decimals
char - singular ASCII characters
string - a sequence, or array, of characters
bool - true/false statements.

Escape Sequences


\b backspace
\t tab
\n linefeed
\f formfeed
\r carriage return
\\ backslash
\" double quote

*/

/*
String type

length() returns the number of characters in this string.
size() Same as length().
at(index) Returns the character at the specified index

Reading strings

getline(cin, s, delimitCharacter)
cin = where we take it from.
s = where we save it
delimitCharacter = where do we stop saving (automatically "\n" unless specified).

*/

/*
This is an IF statement in an expression!

if (x < 1) {
    y = 1;
}

y=(x<1)?1:-1;

these two are the exact same, but the second one is faster to type.


*/


/*
    cmath lib



    Manipulators

    Left

    Fixed

    Setprecision

Setw(width) manipulator!

this commands specifies the minimum number of colums for an output

cout << setw(6) << "C++" << setw(6) "---" << endl;
   C++   ---

right is the default for setw

srand() generates the sed
rand() gives the random number

*/



/*
    Arrays!

double myList[4] = {0.0, 1.0, 2.0, 3.0};
 

*/


/*

vector<int> intVector(10);

Vectors are dynamic! They can grow.


*/